<h2>User info</h2>

<h2>Users</h2>


<% @users.each do |user| %>
<%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no-image.jpg" %>
<%= user.name %>
<%= link_to "show", user_path(user) %>
<% end %>